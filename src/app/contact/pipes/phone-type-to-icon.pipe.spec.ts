import { PhoneTypeToIconPipe } from './phone-type-to-icon.pipe';

describe('PhoneTypeToIconPipe', () => {
  it('create an instance', () => {
    const pipe = new PhoneTypeToIconPipe();
    expect(pipe).toBeTruthy();
  });
});
