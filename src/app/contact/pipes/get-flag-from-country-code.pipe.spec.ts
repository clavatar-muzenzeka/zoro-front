import { GetFlagFromCountryCodePipe } from './get-flag-from-country-code.pipe';

describe('GetFlagFromCountryCodePipe', () => {
  it('create an instance', () => {
    const pipe = new GetFlagFromCountryCodePipe();
    expect(pipe).toBeTruthy();
  });
});
