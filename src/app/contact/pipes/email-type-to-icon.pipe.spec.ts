import { EmailTypeToIconPipe } from './email-type-to-icon.pipe';

describe('EmailTypeToIconPipe', () => {
  it('create an instance', () => {
    const pipe = new EmailTypeToIconPipe();
    expect(pipe).toBeTruthy();
  });
});
